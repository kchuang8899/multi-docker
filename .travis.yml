sudo: required
services:
  - docker

before_install:
  - docker build -t kchuang8899/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true kchuang8899/react-test npm run test -- --coverage

language: generic

after_success:
  - docker build -t kchuang8899/multi-client ./client
  - docker build -t kchuang8899/multi-nginx ./nginx
  - docker build -t kchuang8899/multi-server ./server
  - docker build -t kchuang8899/multi-worker ./worker
  #
  # Log in to the docker CLI
  #
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  #
  # take those images and push them into DockerHub
  #
  - docker push kchuang8899/multi-client
  - docker push kchuang8899/multi-nginx
  - docker push kchuang8899/multi-server
  - docker push kchuang8899/multi-worker
